{
  "name": "@esencia-glow/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Contrato tipado compartido entre la API y el frontend",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "echo \"@esencia-glow/shared: solo tipos, sin tests\" && exit 0"
  }
}

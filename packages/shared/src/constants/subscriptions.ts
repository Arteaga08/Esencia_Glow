/** Un array de ítems de edición sin techo es un DoS del documento contra sí
 * mismo — mismo criterio que MAX_STATUS_HISTORY (commerce.ts). */
const MAX_EDITION_ITEMS = 20;

export { MAX_EDITION_ITEMS };
